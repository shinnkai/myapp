<!DOCTYPE html>
<html>
  <head>
    <title>介護用品レンタルアプリ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1, maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>


  </head>

 <body class="d-flex flex-column vh-100">

    <header class="shadow-sm">
      <% if flash[:notice] %>
  　　　　　　　　<p><%= flash[:notice] %></p>
　　　<% end %>

      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a class="navbar-brand p-3" href="/"><%= link_to 'アプリ', '/', class: 'navbar-brand' %>
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">

            <% if customer_signed_in? %>
              <li class="nav-item">

                <%= link_to 'マイページ', customer_path(current_customer), class: 'btn btn-outline-secondary btn-sm'  %>
              </li>

              <li class="nav-item">
                <%= link_to "About", about_path, class: "btn btn-outline-secondary btn-sm" %>
              </li>

              <li class="nav-item">
                <%= link_to '商品一覧', items_path, class: 'btn btn-outline-secondary btn-sm' %>
              </li>
              <li class="nav-item">

                <%= link_to "カート", cart_items_path(current_customer.id), class: 'btn btn-outline-secondary btn-sm' %>

              </li>
              <li class="nav-item">
                <%= link_to "ログアウト", destroy_customer_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm" %>
              </li>

            <% else %>

              <li class="nav-item">
                <%= link_to "About", about_path, class: "btn btn-outline-secondary btn-sm" %>
              </li>
              <li class="nav-item">
                <%= link_to '商品一覧',  items_path, class: 'btn btn-outline-secondary btn-sm' %>
              </li>
              <li class="nav-item">
                 <%= link_to "新規登録", new_customer_registration_path, class: "btn btn-outline-secondary btn-sm" %>
              </li>
              <li class="nav-item">
                <%= link_to "ログイン", new_customer_session_path, class: "btn btn-outline-secondary btn-sm" %>
              </li>
              
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

  <main class="mb-auto">
  <%= yield %>
  </main>

  <footer class="bg-dark text-white pt-1 pb-1">
    <div class="container">
      <div class="row no-gutters">
        <div class="col-md-4">
          <p></p>

        </div>
          <div class="col-md-6">
          <h6 class="text-white mt-2 mb-3">Link</h6>
          <h6 class="text-white mt-2 mb-3"></h6>
          </ul>
          </div>
      </div>
    </div>
  </footer>
</body>